---
interface Props {
    btnName: string;
    url: string;
}

const { btnName, url } = Astro.props
---
<a 
    href={url}
    class="px-3 sm:px-5 h-12 flex items-center gap-2 bg-slate-500 rounded-md font-bold text-white text-sm md:text-base"
>
    {btnName}
    <slot
    name="after"
    >
</a>
---
interface Props{
    content: string;
    title: string;
}

const { content, title } = Astro.props
---
<!-- TODO, cambiar el style por tailwind -->
<article class="card text-base font-medium block">
	<h1 class="mb-4 text-2xl font-semibold">{title}</h1>
	<p>{content}</p>
</article>
